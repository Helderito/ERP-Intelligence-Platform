import { createManagedReferenceDataService } from "./managedReferenceDataService";

export const unitOfMeasureManagementService = createManagedReferenceDataService(
  "/units-of-measure",
  "Não foi possível concluir o pedido de unidades de medida."
);
